export type TDefaultGlobalLayout = {
  children: React.ReactNode,
}

export type TDefaultGlobalLayoutPosition = {
  children: React.ReactNode,
  header?: boolean,
  shapeRight?: boolean,
  shapeLeft?: boolean
}